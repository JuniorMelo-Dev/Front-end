/* Crie uma função chamda mostrIdade, que recebe nome e idade e retorne a string: `A idade de ${nome} é ${idade}` */
function mostraIdade(nome, idade) {
    return `A idade de ${nome} é ${idade}`;
}

/* Crie uma função chamda mostrCidade, que recebe nome e cidade e retorne a string: `A cidade de ${nome} é ${cidade}` */
function mostraCidade(nome, cidade) {
    return `A cidade de ${nome} é ${cidade}`;
}

/* Crie uma função chamda mostrahobby, que recebe nome e o hobby e retorne a string: `O hobby de ${nome} é ${hobby}` */
function mostraHobby(nome, hobby) {
    return `O hobby de ${nome} é ${hobby}`;
}

/* Importe as funções */
export {mostraIdade, mostraCidade, mostraHobby};